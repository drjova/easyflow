# coding: utf8
def create():
    form = SQLFORM(db.workflow).process()
    if form.accepted:
        response.flash = 'new record inserted'
    return dict(form=form)
def view():
     records = SQLTABLE(db().select(db.workflow.ALL),headers='fieldname:capitalize')
     return dict(records=records)
def edit():
    return 'Hello'
